// src/ejercicio.js
function sumar(a, b) {
  return a + b;
}

// Exporta la función para poder usarla en las pruebas
module.exports = sumar; // Si usas CommonJS
// export function sumar(a, b) { return a + b; } // Si usas ES Modules
